# StockValuator Backend

Stock analysis and management platform API built with FastAPI.

## Setup

```bash
# Install dependencies
uv sync

# Run development server
uv run uvicorn src.main:app --reload

# Run tests
uv run pytest
```

## API Documentation

Once running, visit `/docs` for Swagger UI or `/redoc` for ReDoc.
