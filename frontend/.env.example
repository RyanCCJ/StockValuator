# Backend URL (for API proxy - server side only)
BACKEND_URL=http://localhost:8000

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
AUTH_SECRET=your-auth-secret-at-least-32-characters-long

# Google OAuth (optional - fill in from Google Cloud Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Trust host header (required for proxy environments like Zeabur)
# AUTH_TRUST_HOST=true
